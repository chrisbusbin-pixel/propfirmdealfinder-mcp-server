# Railway deployment config — Prop Firm Deal Finder MCP Server
# Deploy: Connect GitHub repo on railway.com → Auto-detects this file

[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
